# Founder Intelligence — матрица экранов и сквозных переходов

> Historical checkpoint. This matrix was refined by the 2026-08-21/22 verification docs and remains only referenced history. Do not interpret the status below as current.

Дата: 2026-08-17
Ветка: `main`
Принцип: интерфейс только проецирует состояние того же `case_id`; локальный fake/demo state не считается доказательством.

Канонический demo-порядок: `01 → 02 → 03 → 04 → 11 → 12 → 13 → 14 → 05 → 06 → 07 → 08 → 09 → 10`.

| Экран | Действие пользователя | API / graph transition | Ожидаемый видимый результат | Тест / доказательство | Текущий статус |
| --- | --- | --- | --- | --- | --- |
| 01 Dashboard | Выбрать PDF/DOCX/XLSX/CSV/ZIP | API ещё не вызывается; файл остаётся локальным до явного старта | Честный inventory выбранных материалов, без выдуманного проекта и метрик | `upload-entry-visual.test.ts`; screenshot `01-start-dashboard.png` | Работает |
| 02 Data room | Запустить анализ выбранных материалов | `POST /cases` с `auto_start=false` → `POST /documents` с `auto_start=true` → LangGraph primary plan до Gate 2 | Создан private case; виден переход к обработке документов | `founder-workspace-controller.test.ts`; browser case `a4f5c529-c33e-4c6d-bc7c-b89c779d5f2c` | Исправлено и проверено 2026-08-17 |
| 03 Progress / Gate 2 | Просмотреть извлечённый профиль; подтвердить или отклонить | `GET /cases/{case_id}` + `GET /gate2/preview` + `GET /profile`; затем `POST /gate2/decision` с resume token | Source-backed поля, статусы агентов, настоящая остановка перед deep analysis | `test_startup_api_real_deterministic_pdf_upload_journey_reaches_same_case_report_and_trace`; screenshot `founder-order-proof-gate2-1440x1000.png`; Task 8 offline Gates B/C/D-A/D-B/E | PDF → profile доказан; Task 8 offline gate packet now covers restart-safe Gate continuation, while credentialed live smokes remain pending |
| 04 Overview / readiness | Открыть обзор после Gate 2 | Poll того же case; `GET /profile`, затем deep-analysis projections | Продукт, ICP, проблема, readiness и следующий полезный шаг только из реальных полей/секций | `founder-analysis-pages.test.ts`; privacy projection tests | Работает как проекция; полнота зависит от deep result |
| 11 Advisor question | Открыть следующий лучший вопрос | `GET /advisor/next-question` того же `case_id` | Один вопрос с максимальным ожидаемым влиянием и четыре честных режима ответа | Task 2/3 advisor API tests; `founder-advisor-pages.test.ts` | Работает |
| 12 Advisor answer | Ответить вручную, файлом, разрешённым public research или пропустить | `POST /advisor/answers`; research-вариант требует explicit consent и имеет deferred/cached fallback | Сохранён ответ/пропуск, виден безопасный research status; кейс не падает при outage | Task 3 report; advisor controller/API privacy/fallback tests | Работает; live provider smoke отдельно не заявлен |
| 13 Updated analysis | Просмотреть изменения после ответа | Повторное чтение advisor/profile/GTМ projections того же case | Только реально изменившиеся выводы, без выдуманных deltas | `founder-advisor-pages.test.ts`; same-case controller tests; `test_startup_api_file_advisor_answer_restarts_same_case_analysis_without_private_leakage` | Same-case recalculation доказан 2026-08-20: answer запускает revision, invalidates report snapshot, новый profile/report читается в том же `case_id` |
| 14 Improved plan | Принять/отклонить каждое предложение | `GET /advisor/improvements` → `POST /advisor/improvements/{proposal_id}/decision` | Ровно backend proposals, decision/version lineage, без frontend padding и fake progress | advisor presentation/API lineage tests; `test_startup_api_file_advisor_answer_restarts_same_case_analysis_without_private_leakage` | Proposal decision lineage доказан 2026-08-20: accepted proposal создаёт `new_version=2`, revision 4 и новый report без утечки private recommendation в API/runtime |
| 05 Metrics / finance | Открыть метрики; добавить недостающие данные | Чтение profile/readiness/report charts; возврат в data room для новых документов | Подтверждённые/рассчитанные значения или полезный empty state | `founder-analysis-pages.test.ts`; chart/readiness presentation tests | Работает как безопасная проекция |
| 06 Market / competitors | Открыть рынок; при необходимости подготовить consented research | Чтение GTM/report market sections; public research только отдельным consented advisor transition | TAM/SAM/SOM, конкуренты и заменители только при evidence; иначе честный следующий шаг | `founder-strategy-pages.test.ts`; Task 3 research fallback tests; R9 screenshot `06-market-competitors.png` | Работает как безопасная проекция; R9 visual regression закрыла overflow/fallback-псевдоданные |
| 07 Risks / questions | Просмотреть риски и противоречия | Чтение report/readiness contradictions/questions | Evidence-linked risks и один лучший следующий вопрос; без придуманных кейсовых вопросов | `founder-strategy-pages.test.ts`; report/privacy tests; R9 screenshot `07-risks-questions.png` | Работает как безопасная проекция; R9 допускает только `1px` overflow в tolerance |
| 08 Action plan / Gate 3 | Разрешить противоречия и принять направление | `POST /gate3/decision` с явными exclusions → graph resume → report stage | 7/30/60/90 plan того же case и видимое решение Gate 3 | API/graph Gate 3 tests; browser journey | Переход работает; UI выбора конкретных contradiction exclusions остаётся разрывом |
| 09 Report center / Gate 4 | Проверить snapshot; approve/reject freeze | `GET /report` + JSON/HTML; `POST /gate4/decision`; после approve `GET /report/pdf` | Same-case JSON/HTML/PDF и lineage одного snapshot | report E2E/API/browser tests; R9 browser evidence; `test_startup_api_file_advisor_answer_restarts_same_case_analysis_without_private_leakage`; Task 8 offline Gates B/C/D-A/D-B/E | Same-case JSON/HTML/PDF и Gate 4 approved доказаны в R9/focused API/offline gates; live LangSmith/OpenAI smokes ещё не выполнены, поэтому Task 8 не закрыт |
| 10 Admin observability | Выбрать тот же case/run | Streamlit читает local audit; optional sanitized LangSmith exporter health | Graph nodes, tools, retries, errors, gates, token/cost, lineage без private payload | `test_streamlit_admin_console.py`; sanitized trace tests; `tests/evaluation/test_startup_trace_sidecar.py`; R9 browser evidence; Stage C LangSmith-focused tests | Local audit/Admin trace работает для того же `case_id`; sanitized LangSmith mapping covered offline, credentialed live trace `armed_not_executed`; Admin owner-final acceptance не заявлен |

## 2026-08-20 post-R9 evidence refresh

- Visual: fresh 14-state Founder Workspace capture at `1440x1000` is stored under `artifacts/ui/frontend-stabilization-final-01a01fd5-r9/`; normalized independent review passed, screen 06 overflow is closed, and Admin remains provisional.
- Functional restart: `tests/api/test_startup_api.py::test_startup_api_restart_resumes_revised_same_case_thread_without_duplicate_public_research` passed. It proves same-case restart/revision/report lineage after advisor answer and proposal acceptance; its public-research counter is deterministic frozen fallback evidence, not a live external provider proof. Focused API run passed: `3 passed, 1 warning in 19.89s`.
- Multi-agent/trace boundary: Stage C LangSmith-focused suite passed (`16 passed in 35.04s`) for typed Plan-and-Execute roles, tool boundaries, fallback, Gates, restart, local audit, and sanitized LangSmith mapping.
- Full local green: frontend `npm test`, `npm run typecheck`, `npm run lint`, and `npm run build` passed; backend/static verification passed with Ruff clean, strict mypy clean (`244 source files`), and full pytest `1445 passed, 1 skipped, 1 warning in 326.78s`.
- Offline Task 8 gates: Gates B/C/D-A/D-B/E passed under `.local/post-visual-final-01a01fd5-20260820-01`; D-A/D-B semantic fingerprints were `4/4` equal and persisted fingerprints were `4/4` equal.
- Failure matrix: r2 passed under `.local/post-visual-final-01a01fd5-20260820-01/failure-matrix-r2` with 13 proof tests, no live calls, and `sha256:c0bd16ea3ea47099e4bf22918c8eadc97f6477b8c0fbb7b3cdac63f4a01b23e8`. The first failure was a transient Windows SQLite lock; the exact failing test and r2 matrix passed afterward.
- Live-smoke blocker: `LANGSMITH_API_KEY` and `OPENAI_API_KEY` are present, but both credentialed smokes are `armed_not_executed` pending explicit owner approval for external transmission. Do not mark Task 8, Queue 5, or Sellable Demo complete from offline evidence alone.

## Порядок закрытия оставшихся функциональных разрывов

1. Получить явное разрешение владельца на один sanitized LangSmith live smoke: внешний вызов передаст только безопасную trace metadata вроде case/run/report IDs и checksums, без private document content.
2. Получить явное разрешение владельца на один bounded OpenAI competitor-synthesis smoke: внешний вызов передаст sanitized `StartupProfile` и frozen competitor/source summaries, budget `<= $0.25`, без live web research.
3. После live-smoke evidence и независимого approve обновить demo/verification docs и только тогда формулировать Task 8 / Queue 5 / Sellable Demo completion.
